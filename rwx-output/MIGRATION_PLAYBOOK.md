# 🚀 RWX Migration Playbook

Generated by **rwx-migrate** — built by Jafar (github.com/jafar-97/rwx-migrate)

---

## 📊 Summary

| Metric | Value |
|--------|-------|
| GHA workflows analyzed | 12 |
| Jobs converted to RWX tasks | 22 |
| Estimated build time saving | ~60% faster builds (content-based caching + true parallelism) |
| Parallelization | All independent jobs will run concurrently on separate machines |

---

## ⚡ Why RWX Will Be Faster

| GitHub Actions | RWX |
|----------------|-----|
| Linear jobs, manual parallelism | DAG: true dependency-based parallelism |
| Cache config is manual & fragile | Content-based caching — automatic |
| Full job reruns on failure | Granular task retries |
| Per-minute billing, over-provisioned | Per-second billing, right-sized machines |

---

## 🔧 Simplification Wins

- [analyze] actions/checkout → use RWX's built-in git clone (no action needed)
- [analyze] Matrix job detected → RWX parallelizes these natively across machines without matrix config
- [sync-downstream] actions/checkout → use RWX's built-in git clone (no action needed)
- [sync-downstream] actions/setup-node@v6 → pin runtime in RWX machine config or use a pre-built image
- [sync-downstream] Matrix job detected → RWX parallelizes these natively across machines without matrix config
- [validate] actions/checkout → use RWX's built-in git clone (no action needed)
- [validate] actions/setup-node@v6 → pin runtime in RWX machine config or use a pre-built image
- [notify] actions/checkout → use RWX's built-in git clone (no action needed)
- [publish] actions/checkout → use RWX's built-in git clone (no action needed)
- [publish-canary] actions/checkout → use RWX's built-in git clone (no action needed)
- [check] actions/checkout → use RWX's built-in git clone (no action needed)
- [check] actions/setup-node@v6 → pin runtime in RWX machine config or use a pre-built image
- [autofix] actions/checkout → use RWX's built-in git clone (no action needed)
- [build] actions/checkout → use RWX's built-in git clone (no action needed)
- [typecheck] actions/checkout → use RWX's built-in git clone (no action needed)
- [typecheck-www] actions/checkout → use RWX's built-in git clone (no action needed)
- [test] actions/checkout → use RWX's built-in git clone (no action needed)
- [e2e] actions/checkout → use RWX's built-in git clone (no action needed)
- [e2e-deno] actions/checkout → use RWX's built-in git clone (no action needed)
- [e2e-bun] actions/checkout → use RWX's built-in git clone (no action needed)
- [e2e-legacy-node] actions/checkout → use RWX's built-in git clone (no action needed)
- [e2e-legacy-node] actions/setup-node@v6 → pin runtime in RWX machine config or use a pre-built image
- [release-tmp] actions/checkout → use RWX's built-in git clone (no action needed)
- [monotest-build] actions/checkout → use RWX's built-in git clone (no action needed)
- [e2e] Matrix job detected → RWX parallelizes these natively across machines without matrix config
- [e2e-deno] Matrix job detected → RWX parallelizes these natively across machines without matrix config
- [e2e-bun] Matrix job detected → RWX parallelizes these natively across machines without matrix config
- [e2e-legacy-node] Matrix job detected → RWX parallelizes these natively across machines without matrix config
- [test] actions/checkout → use RWX's built-in git clone (no action needed)

---

## ✅ Cutover Checklist

- [ ] Review generated `tasks.yml` — verify commands match your expectations
- [ ] Replace any `# GHA action:` comments with equivalent shell commands
- [ ] Connect your repo to RWX cloud (Settings → Integrations → GitHub)
- [ ] Run `rwx run --file tasks.yml` locally to validate
- [ ] Run RWX side-by-side with GHA for 1 week to compare results
- [ ] Disable GHA workflows once RWX is confirmed stable

---

## 📁 Generated Files

- `tasks.yml` — your RWX workflow (ready to run)
- `MIGRATION_PLAYBOOK.md` — this file

---

*Built to solve RWX's #1 customer onboarding bottleneck.*
*Every manual migration currently takes an RWX engineer 2-4 hours. This does it in seconds.*